# Core Django Framework
Django==4.2
asgiref==3.8.1
sqlparse==0.5.3
tzdata==2024.2
pytz==2024.2

# Database Drivers
psycopg2-binary==2.9.10  # PostgreSQL driver for Azure Database
# mysqlclient==2.0.3  # Uncomment if using MySQL instead

# Web Server (Production)
gunicorn==20.0.4  # WSGI HTTP Server for Azure App Service
setuptools>=68.0.0,<81  # Fix: provides pkg_resources required by gunicorn (versions 81+ dropped pkg_resources)
whitenoise==6.11.0  # Static file serving

# REST Framework
djangorestframework==3.14.0
djangorestframework-simplejwt==5.2.2

# Payment Processing
razorpay==1.2.0  # Razorpay payment gateway integration

# Image Processing
Pillow==11.0.0  # For course thumbnails and payment screenshots

# Forms & UI
django-widget-tweaks==1.4.8  # Template form rendering
django-crispy-forms==1.10.0  # Bootstrap form styling

# Rich Text Editor (for course descriptions)
django-ckeditor==6.0.0
django-js-asset==1.2.2

# Utilities
python-decouple==3.8  # Environment variable management (preferred for Django)
python-dotenv==1.0.1  # Alternative environment variable management
python-bidi==0.4.2  # Bi-directional text support
arabic-reshaper==3.0.0  # Arabic text support (updated version)
requests==2.32.3  # HTTP library
certifi==2025.8.3  # SSL certificates
urllib3==2.5.0  # HTTP client
six==1.16.0  # Python 2/3 compatibility

# PDF Generation (for certificates/invoices)
reportlab==4.4.4  # Use pre-built wheel version
PyPDF2==1.26.0
xhtml2pdf==0.2.5
html5lib==1.1
webencodings==0.5.1

# SMS Notifications (Twilio)
twilio==9.5.0  # Updated version compatible with newer PyJWT
PyJWT==2.8.0  # Compatible with both djangorestframework-simplejwt and newer twilio

# Azure Integration (Optional - for Blob Storage)
# azure-storage-blob==12.19.0  # Uncomment for Azure Blob Storage
# django-storages[azure]==1.14  # Uncomment for Azure Storage backend

# Development Tools (Comment out for production)
# python-decouple==3.8  # Separate settings for dev/prod

# Security
# django-cors-headers==4.3.1  # Uncomment if using CORS

# Performance Optimization (Optional)
# django-redis==5.4.0  # Uncomment for Redis caching
# redis==5.0.1  # Uncomment for Redis session storage

# Monitoring (Optional for Azure Application Insights)
# opencensus-ext-azure==1.1.13  # Uncomment for App Insights
# opencensus-ext-django==0.8.0  # Uncomment for Django tracing
